services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: nexaforge_dz
    ports:
      - "5432:5432"
    volumes:
      - nexa_pg_data:/var/lib/postgresql/data
volumes:
  nexa_pg_data:
